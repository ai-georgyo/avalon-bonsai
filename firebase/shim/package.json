{
  "name": "avalon-firebase-shim",
  "version": "1.0.0",
  "private": true,
  "description": "Tree-shaken Firebase v12 modular shim for the avalon-bonsai client (populates window.__fb).",
  "dependencies": {
    "firebase": "^12.15.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "build": "esbuild entry.mjs --bundle --minify --format=iife --outfile=../../web/firebase-shim.js"
  }
}
